version: 2

models:
  - name: session_summary
    columns:
      - name: sessionID
        data_tests:
          - unique
          - not_null
          - dbt_expectations.expect_column_values_to_match_regex:
              regex: "^s[0-9]+$"
